import React, { useReducer } from 'react';



export default function PlayerApp ({ onReset }) {
  return (
    <div>
      <p>Sorry, nothing here yet, chief!</p>
      <button onClick={onReset}>Go Back</button>
    </div>
  );
}
